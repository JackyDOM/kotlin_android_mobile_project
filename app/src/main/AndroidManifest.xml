<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFirstApp"
        tools:targetApi="31">
        <activity
            android:name=".Home_Screen.BookPdfActivity"
            android:exported="false" />
        <activity
            android:name=".Home_Screen.BookDetailActivity"
            android:exported="false" />
        <activity
            android:name=".Home_Screen.GridBooksActivity"
            android:exported="false" />
        <activity
            android:name=".ButtonNavigationBar_Screen.MainActivityButtomnavScreen"
            android:exported="false" />
        <activity
            android:name=".Logo_Screen.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignIn_Screen.SignInActivity" />
        <activity android:name=".SignUp_Screen.SignUpActivity" />
        <activity android:name=".Author_Screen.AuthorInformationActivity" />
    </application>

</manifest>